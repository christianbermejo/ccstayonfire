{
  "scripts": {
    "generate-config": "node -r dotenv/config scripts/generate-firebase-config.js",
    "build": "npm run generate-config && rm -rf dist && cp -R src dist"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}
